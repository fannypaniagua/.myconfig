sudo apt-get install debhelper bum
make
sudo make install
sudo cp debian/init.d /etc/init.d/macfanctld
sudo chmod +x /etc/init.d/macfanctld
sudo bum

Then use bootup-manager (bum) to start macfanctld automatically at startup
